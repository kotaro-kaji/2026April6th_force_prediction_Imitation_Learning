まずタスクの成功動作、デモデータの収集のため、異なる物性のオブジェクトをいくつか用意する。  
 例えば、未知重心のオブジェクトをpinchingで持ち上げるタスクでは３つ程度。  
デモデータは２種類収集する。  
 １つ目は、人間が眼の前のオブジェクトがどういう内部物性なのかを知っており、すぐに正解行動を取れるタイプ。デモデータAと呼ぶ。  
 ２つ目は、人間が眼の前のオブジェクトがどういうオブジェクトなのかを知らず、かならず最初に探索行動を取るデモデータ。デモデータBと呼ぶ。  
まず最初にデモデータから内部物性表現の獲得のための学習を行う。ここでは、探索行動のある、デモデータBのみを用いる。  
まず、Diffusion model ないし、Flow Matching model ないし、多峰性の出力が可能なモデルによって、未来の画像と力を予測する。  
image_t+1:t+H, force_t+1:t+H = 未来想像モデル（o_t, k, noised_o_t_k, action_t:t+H-1, 内部物性ベクトル）  
ここでの内部物性ベクトルは、Parametric バイアス的に学習される。１つのオブジェクトあたり、１つのnn.embedding のベクトルを持つ。未来想像モデルと、このPBは未来想像タスクを通して、両者に勾配更新がかけられ、１つのオブジェクトあたり１つの内部物性ベクトルを得る。  
次に、同じくデモデータBに対して、仮想的にオンラインに内部物性補正をする。デモデータBの開始時点では、内部物性は不明であり、３つの内部物性表現の平均値ベクトルであると仮定する。仮定された内部物性表現ベクトルを用いて、未来想像を行う。未来想像は外れることが予測されるため、その誤差の責任は内部物性表現ベクトルのみにあるとして、内部物性表現ベクトルのみにbackpropする。こうして、時系列的に、エピソード内での時間が経過するごとに内部物性表現ベクトルがオンライン更新されたデータが得られる。  
デモデータAでは、オブジェクト１つあたり、内部物性表現は固定されているとして、データの全てのtime step で固定の内部物性表現とする。
次にデモデータA、デモデータBにおいて、内部物性表現ベクトルをconditioning (stateの一部)として、DPを学習させる。内部物性表現が平均化されているケースでは、探索行動を行い、内部物性表現が平均化されていない（すでに同定が終わっている）ケースでは、探索行動をやめて、すぐさまタスクを実行するポリシーが得られる。

やはりデータセットを分けないとだ。
・物性は失敗行動から学ぶ。
・模倣学習では失敗行動だけを教えるわけには行かない。失敗への対処は教えるが、失敗していない正解行動のみのデータも必要になる。